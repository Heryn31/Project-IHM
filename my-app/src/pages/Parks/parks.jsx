import React from 'react'
import Navbar from '../../components/Navbar'

function parks() {
  return (
    <div>
      <Navbar theme="light" toggleTheme={() => {}} showCenter={false} />
    </div>
  )
}

export default parks
